image: docker:stable

services:
  - docker:dind

variables:
  DOCKER_REPOSITORY: $CI_REGISTRY/archomeda/docker-volume-backup

before_script:
  - docker version
  - docker info
  - docker login -u $CI_REGISTRY_USER -p $CI_REGISTRY_PASSWORD $CI_REGISTRY

stages:
  - build

build:
  stage: build
  only:
    - master
  script:
    - docker build -t $DOCKER_REPOSITORY:latest .
    - docker push $DOCKER_REPOSITORY:latest
